# 项目日志

> **日期说明**：历史条目标注的日期为 2024-01-15，实际开发时间约为
> 2025-11 至 2026-01（以文件时间戳与历史设计线程为准）。2026-08-07 整理
> 文档时统一修正认知，未逐条改写历史记录。

## 2024-01-15 - 初始项目创建

### 创建的基础文件
- `src/models.py`：数据模型（Station, ProductionLine, Alert, SimulationState）
- `src/simulation.py`：仿真引擎（SimulationEngine）
- `src/gui_main.py`：主窗口（MainWindow）
- `src/gui_canvas.py`：画布视图（CanvasView）
- `src/gui_panels.py`：面板组件（ConfigPanel, KPIDashboard, AlertPanel, StationDialog）
- `src/utils.py`：工具函数
- `src/main.py`：程序入口

### 创建的文件
- `tests/test_basic.py`：基础功能测试脚本
- `tests/测试报告.md`：详细测试报告
- `run.py`：便捷启动脚本

### 下一步
1. 运行GUI程序进行实际测试
2. 验证GUI功能是否正常
3. 根据测试结果修复问题

## 2024-01-15 - Bug修复

### 修复的问题

1. **添加工序问题**
   - 问题：第一个工序可以添加，后续工序页面无响应
   - 原因：ConfigPanel没有保存production_line引用，ID生成不正确
   - 修复：添加production_line引用，修复ID生成逻辑，修复_on_select方法

2. **编辑/删除问题**
   - 问题：已添加的工序无法编辑、删除
   - 原因：get_selected_station()返回的selected_station没有正确设置
   - 修复：修复_on_select方法，正确从production_line中查找Station对象

3. **仿真重启问题**
   - 问题：停止仿真后无法再次进行仿真
   - 原因：仿真引擎没有完全清理，画布状态没有重置
   - 修复：添加完整的清理逻辑，重置所有状态

### 修改的文件
- `src/gui_panels.py` - 修复ConfigPanel和StationDialog
- `src/gui_main.py` - 修复仿真停止逻辑

### 测试结果
所有问题已修复，功能正常：
- ✅ 可以正常添加多个工序
- ✅ 可以正常编辑和删除工序
- ✅ 可以正常停止和重新启动仿真

详细修复记录见：`tests/问题修复记录.md`

## 2024-01-15 - 功能优化

### 优化的功能

1. **工序显示窗口自适应（蛇形布局）**
   - 问题：工序数量超出展示范围时无法显示
   - 优化：实现蛇形布局，自动计算每行最大工序数，超出时自动换行
   - 特性：
     - 自动计算画布宽度，确定每行最大工序数
     - 奇数行反向排列（Z字形效果）
     - 连接线支持曲线（跨行时使用曲线，同行时使用直线）
     - 自动居中显示所有行

2. **仿真时间显示优化**
   - 问题：1倍速率时时间变化不够直观
   - 优化：
     - 1倍速时按真实时间推进（1秒仿真时间 = 1秒真实时间）
     - 状态栏显示当前仿真时间（时:分:秒格式）
     - 1倍速时更新频率为每1秒，其他速度下为每10秒

3. **报警信息时间戳**
   - 问题：报警信息没有时间标识
   - 优化：
     - Alert类添加timestamp_minutes字段
     - 所有报警创建时记录从仿真开始经过的分钟数
     - 报警面板显示格式：`[XX.X分钟] [级别] 消息内容`
     - 瓶颈监控和WIP监控都添加时间戳

### 修改的文件
- `src/models.py` - Alert类添加timestamp_minutes字段
- `src/gui_canvas.py` - 实现蛇形布局和曲线连接线
- `src/simulation.py` - 优化时间推进逻辑，添加时间戳到报警
- `src/gui_panels.py` - 报警面板显示时间戳
- `src/gui_main.py` - 状态栏显示仿真时间

### 技术细节

**蛇形布局算法**：
- 计算每行最大工序数：`max_stations_per_row = (canvas_width - 2*start_x) / station_spacing`
- 计算行数：`num_rows = (num_stations + max_stations_per_row - 1) // max_stations_per_row`
- 奇数行反向：`if row % 2 == 1: col = max_stations_per_row - 1 - col`

**1倍速时间控制**：
- 使用time.time()记录真实时间
- 每次推进1秒仿真时间后sleep 1秒真实时间
- 确保仿真时间与真实时间同步

**曲线连接线**：
- 跨行时使用二次贝塞尔曲线绘制平滑过渡
- 同行时使用直线连接
- 自动计算控制点生成平滑曲线

## 2024-01-15 - 功能完善和优化

### 新增和改进的功能

1. **速度倍数控制优化**
   - 明确速度要求：
     - 1倍速：1秒真实时间 = 1秒仿真时间
     - 2倍速：1秒真实时间 = 2秒仿真时间
     - 8倍速：1秒真实时间 = 8秒仿真时间
     - 16倍速：1秒真实时间 = 16秒仿真时间
   - 修复：所有速度倍数都按真实时间比例推进
   - 添加2倍速选项到速度下拉框

2. **S型布局优化**
   - 问题：箭头顺序错误，直线穿过工序图示
   - 修复：
     - 优化蛇形布局算法，确保工序顺序正确
     - 连接线从节点边缘开始，不穿过节点
     - 根据方向确定连接点（左/右/上/下边缘）
     - 跨行时使用平滑曲线，箭头方向正确
     - 增加边缘偏移量，避免连接线贴边

3. **班次配置功能**
   - 新增：班次配置对话框
   - 功能：
     - 可配置班次时长（小时）
     - 可配置休息时间（分钟）
     - 可配置工人时薪（元/小时）
   - 位置：控制面板添加"班次配置"按钮
   - 仿真时自动使用配置的班次时长

4. **报警信息清空逻辑**
   - 逻辑：
     - 停止后重新开始仿真 → 清空报警信息
     - 暂停后继续仿真 → 不清空报警信息
   - 实现：通过判断simulation_engine状态区分新仿真和恢复

### 修改的文件

- `src/simulation.py`
  - `_run_simulation_thread()`：优化所有速度倍数的时间推进逻辑
  - `pause()`和`resume()`：添加注释说明暂停/恢复机制

- `src/gui_canvas.py`
  - `update_production_line()`：优化蛇形布局算法
  - `_draw_connection_curved()`：优化连接线绘制，避免穿过节点

- `src/gui_main.py`
  - `_create_main_layout()`：添加"班次配置"按钮
  - `_btn_start_simulation()`：实现报警清空逻辑和暂停恢复逻辑
  - `_btn_config_shift()`：新增班次配置对话框调用

- `src/gui_panels.py`
  - `ShiftConfigDialog`：新增班次配置对话框类

### 技术细节

**速度倍数控制**：
```python
# 加速模式：根据速度倍数控制时间推进
target_sim_time = elapsed_real_time * speed
sleep_time = step_size / speed  # 根据速度计算需要sleep的真实时间
```

**S型布局优化**：
- 分三步：先计算位置，再绘制节点，最后绘制连接线
- 连接点从边缘开始，偏移量 = STATION_WIDTH/2 + 5像素
- 根据方向向量确定连接点位置（左/右/上/下边缘）

**报警清空逻辑**：
- 判断条件：`is_new_simulation = (engine is None or not engine.is_running)`
- 暂停状态：`engine.is_paused = True`且`engine.is_running = True`
- 恢复时调用`resume()`，不清空报警

### 测试建议

1. **速度倍数测试**：
   - 测试1倍、2倍、8倍、16倍速度
   - 观察状态栏时间是否按正确比例推进

2. **S型布局测试**：
   - 添加10个以上工序
   - 观察连接线是否正确，是否穿过节点
   - 观察箭头方向是否正确

3. **班次配置测试**：
   - 点击"班次配置"按钮
   - 修改班次时长、休息时间、时薪
   - 运行仿真，验证是否使用新配置

4. **报警清空测试**：
   - 运行仿真，生成报警
   - 点击停止，再点击开始 → 应该清空报警
   - 运行仿真，生成报警
   - 点击暂停，再点击开始 → 应该保留报警

## 2024-01-15 - 新增UPPH指标

### 新增功能

**UPPH（Units Per Person per Hour）指标**
- 功能：添加人均效率指标UPPH，用于衡量单位时间内每人能生产的成品数量
- 计算公式：UPPH = 瓶颈产能 / 总工人数（颗/人·小时）
- 显示位置：KPI仪表盘，新增第6列显示
- 显示格式：保留1位小数，单位"颗/人·h"

**UPPH的意义**：
- 衡量人均产出效率：数值越高，说明人力利用效率越高
- 评估产线设计合理性：UPPH过低可能表示人力配置不合理
- 跟踪培训效果：UPPH提升说明员工技能提升或流程优化
- 典型范围：电子烟行业UPPH通常在30-80颗/人·小时

### 修改的文件

- `src/models.py`
  - `ProductionLine.calculate_upph()`：新增UPPH计算方法
  - 计算公式：瓶颈产能 / 总工人数
  - 包含完整的文档注释和示例说明

- `src/gui_panels.py`
  - `KPIDashboard._create_widgets()`：KPI列表从5列扩展到6列
  - `KPIDashboard.update_kpis()`：添加UPPH更新逻辑
  - 显示格式：保留1位小数

- `src/gui_main.py`
  - `_update_kpi()`：添加UPPH计算和更新调用

### 技术细节

**UPPH计算公式选择**：
- 使用"瓶颈产能 / 总工人数"而非"日产量 / (总工人数 × 有效工作时间)"
- 原因：产线实际产出受瓶颈限制，用瓶颈产能更准确反映"在瓶颈约束下的人均效率"
- 两种公式在数学上等价，但第一种更直观

**边界情况处理**：
- 产线为空：返回0.0
- 总工人数为0：返回0.0（避免除零错误）

### 测试建议

1. **UPPH计算测试**：
   - 创建包含多个工序的产线
   - 观察UPPH值是否合理（通常在30-80范围内）
   - 修改工人数量，观察UPPH变化

2. **KPI显示测试**：
   - 确认KPI仪表盘显示6个指标
   - 确认UPPH显示格式正确（保留1位小数）
   - 确认UPPH值实时更新

## 2024-01-15 - 完善Excel导入功能

### 新增功能

**Excel导入功能完善**
- 功能：从Excel文件批量导入产线配置
- 菜单位置：文件 → 导入Excel
- 支持格式：.xlsx 和 .xls
- 数据校验：完善的错误检查和提示

**Excel模板格式**：
- 必填字段：工序名、耗时(秒)、人数
- 可选字段：模式（默认"并联"）、OEE（默认0.85）、人员效率（默认0.95）、切换时间（默认45分钟）、缓冲区容量（默认100）
- 支持多种列名变体（中英文、大小写等）

**功能特点**：
- 智能列名识别：支持多种列名格式（如"工序名"、"工序名称"、"name"等）
- 详细错误提示：显示具体哪一行、哪个字段有问题
- 部分导入支持：即使部分行有错误，也能导入成功的数据
- 数据校验：导入后自动校验产线配置合法性

### 修改的文件

- `src/utils.py`
  - `import_from_excel()`：完全重写，添加详细的数据校验和错误处理
  - `create_excel_template()`：新增函数，用于创建Excel模板文件
  - 返回值改为元组：`(ProductionLine, error_msg)`，支持警告信息

- `src/gui_main.py`
  - `_menu_import_excel()`：新增菜单处理方法
  - 文件菜单添加"导入Excel"选项
  - 导入成功后显示警告对话框（如果有部分错误）

- `configs/产线配置模板.xlsx`
  - 新增Excel模板文件，包含示例数据

### 技术细节

**列名映射机制**：
- 支持多种列名变体，提高兼容性
- 自动识别中英文列名
- 忽略大小写和空格

**错误处理策略**：
- 逐行处理，单行错误不影响其他行
- 收集所有错误，统一显示
- 区分致命错误（无法导入）和警告（部分成功）

**数据校验流程**：
1. 文件存在性检查
2. Excel格式检查
3. 必填字段检查
4. 数据类型校验
5. 数值范围校验
6. 工序参数校验
7. 产线配置完整性校验

### 使用说明

1. **准备Excel文件**：
   - 参考 `configs/产线配置模板.xlsx`
   - 至少包含：工序名、耗时(秒)、人数三列
   - 其他字段可选，会使用默认值

2. **导入步骤**：
   - 点击"文件" → "导入Excel"
   - 选择Excel文件
   - 查看导入结果和警告信息

3. **常见问题**：
   - 如果缺少必填列，会提示具体缺少哪些列
   - 如果数据格式错误，会显示具体行号和错误原因
   - 如果部分行有错误，会显示警告但继续导入成功的数据

### 测试建议

1. **正常导入测试**：
   - 使用模板文件导入
   - 验证所有工序是否正确导入
   - 验证可选字段的默认值是否正确

2. **错误处理测试**：
   - 测试缺少必填列的情况
   - 测试数据格式错误的情况
   - 测试部分行有错误的情况

3. **兼容性测试**：
   - 测试不同的列名格式
   - 测试中英文列名混合
   - 测试大小写不同的列名

## 2024-01-15 - 实现方案对比功能

### 新增功能

**方案对比功能**
- 功能：保存多个产线配置方案，并进行KPI对比
- 方案数量限制：至少2个，至多3个
- 数据存储：内存存储（程序关闭后丢失）
- 对比方式：表格形式，显示KPI和差异

**核心功能**：
1. **保存方案**：
   - 保存当前产线配置为命名方案
   - 输入方案名称（必填）和描述（可选）
   - 自动检查方案数量限制（最多3个）
   - 使用深拷贝确保方案数据独立性

2. **方案对比**：
   - 对比所有已保存方案的KPI
   - 显示7个KPI指标：总人数、瓶颈产能、日产量、日成本、单颗成本、产线平衡率、UPPH
   - 计算差异（相对于第一个方案的绝对值和百分比）
   - 推荐最佳方案（默认：单颗成本最低）

**UI组件**：
- `SaveScenarioDialog`：保存方案对话框
- `ScenarioCompareDialog`：方案对比对话框（使用ttk.Treeview表格）
- 控制面板添加"保存方案"按钮
- 分析菜单 → 方案对比

### 修改的文件

- `src/models.py`
  - `Scenario`类：新增方案数据模型
  - 包含方案名称、产线配置快照、创建时间、描述
  - `get_kpis()`方法：计算所有KPI指标
  - `create()`工厂方法：自动处理深拷贝和时间戳

- `src/scenario_manager.py`（新增文件）
  - `ScenarioManager`类：方案管理器
  - 方案数量限制：最多3个，至少2个才能对比
  - `save_scenario()`：保存方案（带验证）
  - `delete_scenario()`：删除方案
  - `compare_scenarios()`：对比方案KPI
  - `recommend_best_scenario()`：推荐最佳方案

- `src/gui_panels.py`
  - `SaveScenarioDialog`：保存方案对话框类
  - `ScenarioCompareDialog`：方案对比对话框类
  - 使用ttk.Treeview显示对比表格
  - 显示差异和推荐方案

- `src/gui_main.py`
  - 添加`scenario_manager`实例
  - `_btn_save_scenario()`：保存方案按钮处理
  - `_menu_compare_solutions()`：方案对比菜单处理
  - 控制面板添加"保存方案"按钮

### 技术细节

**方案快照机制**：
- 使用`copy.deepcopy()`深拷贝产线配置
- 确保后续修改不影响已保存的方案
- 方案数据完全独立

**对比逻辑**：
- 以第一个方案为基准
- 计算其他方案的绝对差异和百分比差异
- 差异格式：`+425 (+50%)` 或 `-0.09 (-26%)`

**推荐算法**：
- 默认推荐单颗成本最低的方案
- 同时检查其他优势（产能最高、平衡率最高、UPPH最高）
- 显示推荐理由

**方案数量限制**：
- 最多保存3个方案
- 至少2个方案才能对比
- 达到上限时提示用户删除方案

### 使用说明

1. **保存方案**：
   - 配置好产线后，点击"保存方案"按钮
   - 输入方案名称（必填）和描述（可选）
   - 点击"确定"保存

2. **方案对比**：
   - 保存至少2个方案后，点击"分析" → "方案对比"
   - 自动对比所有已保存的方案
   - 查看KPI对比表格和推荐方案

3. **方案管理**：
   - 方案保存在内存中，程序关闭后丢失
   - 如需持久化，可以手动保存为JSON文件

### 测试建议

1. **保存方案测试**：
   - 创建产线配置
   - 保存方案A
   - 修改配置，保存方案B
   - 验证方案数量限制（最多3个）

2. **方案对比测试**：
   - 保存2-3个方案
   - 打开方案对比对话框
   - 验证KPI计算是否正确
   - 验证差异计算是否正确
   - 验证推荐方案是否合理

3. **数据独立性测试**：
   - 保存方案后修改产线配置
   - 验证已保存的方案不受影响

## 2024-01-15 - 修复GUI Bug

### 修复的问题

**Bug 1: 保存方案弹窗位置问题**
- 问题：弹窗无法在界面居中，随机出现在界面下方或左侧，偶尔点击无响应
- 原因：
  - 对话框位置计算在创建widgets之前，导致尺寸不准确
  - 使用屏幕居中而非父窗口居中
  - 没有边界检查，可能超出屏幕
- 修复：
  - 先创建所有widgets，再计算位置
  - 改为相对于父窗口居中
  - 添加屏幕边界检查，确保对话框不会超出屏幕
  - 同时修复了方案对比对话框的相同问题

**Bug 2: 画布自适应问题**
- 问题：产线布局不随窗口大小调整，窗口变化时显示不完全
- 原因：
  - 画布没有监听窗口大小变化事件
  - 布局计算使用固定值，没有使用实际画布尺寸
- 修复：
  - 添加`<Configure>`事件绑定，监听画布大小变化
  - 实现`_on_canvas_resize()`方法处理大小变化
  - 实现`_redraw_production_line()`方法重新绘制
  - 使用`winfo_reqwidth()`和`winfo_reqheight()`获取实际画布尺寸
  - 添加防抖机制，避免频繁重绘

### 修改的文件

- `src/gui_panels.py`
  - `SaveScenarioDialog.__init__()`：修复对话框居中逻辑
  - `ScenarioCompareDialog.__init__()`：修复对话框居中逻辑
  - 改为相对于父窗口居中，而不是屏幕居中
  - 添加屏幕边界检查

- `src/gui_canvas.py`
  - `__init__()`：添加`<Configure>`事件绑定
  - `_on_canvas_resize()`：新增画布大小变化事件处理
  - `_redraw_production_line()`：新增重绘方法
  - `update_production_line()`：改进布局计算，使用实际画布尺寸
  - 添加防抖机制，避免频繁重绘

### 技术细节

**对话框居中算法**：
```python
# 获取父窗口位置和大小
parent_x = parent.winfo_x()
parent_y = parent.winfo_y()
parent_width = parent.winfo_width()
parent_height = parent.winfo_height()

# 计算居中位置（相对于父窗口）
x = parent_x + (parent_width - dialog_width) // 2
y = parent_y + (parent_height - dialog_height) // 2

# 确保对话框不会超出屏幕
x = max(0, min(x, screen_width - dialog_width))
y = max(0, min(y, screen_height - dialog_height))
```

**画布自适应机制**：
- 监听`<Configure>`事件（画布大小变化时触发）
- 使用`after_idle()`延迟重绘，避免频繁触发
- 使用`after_cancel()`取消之前的延迟任务（防抖）
- 重新计算布局时使用实际画布尺寸

### 测试建议

1. **对话框居中测试**：
   - 打开保存方案对话框，验证是否在父窗口居中
   - 打开方案对比对话框，验证是否在父窗口居中
   - 调整主窗口位置，验证对话框是否跟随居中

2. **画布自适应测试**：
   - 调整窗口大小，验证产线布局是否自动调整
   - 缩小窗口，验证工序是否自动换行
   - 放大窗口，验证工序是否充分利用空间
   - 验证调整窗口时不会出现闪烁或卡顿

## 2026-08-07 - Git 初始化与文档整理

### 版本管理
- 初始化 Git 仓库（main 分支），添加 `.gitignore`
  （忽略 `__pycache__/`、`.DS_Store`、`.trae/`、IDE 与运行时产物）
- 首次提交保存项目现状快照

### 文档更新
- 重写 `README.md`：补齐项目结构、功能清单、文档索引、速度选项、版本历史
- 重写 `docs/项目成熟度分析.md`：对齐当前实现状态，明确待办优先级
- 更新 `docs/方案对比功能设计.md`：标注实现现状（2-3 个方案，PRD 目标 5 个）
- 更新 `docs/开发指南.md`：补充 scenario_manager.py 模块说明

### 文件清理
- 删除冗余测试记录文档：`tests/测试报告.md`、`tests/问题修复记录.md`、
  `tests/功能优化记录.md`（历史内容与本日志重叠，已由本日志保留要点）
- 删除 IDE 规划文档目录 `.trae/documents/`（评估内容已并入成熟度分析）
- 删除 `src/__pycache__/`、`.DS_Store`、根目录空 `docs/` 目录

### 验证
- `python tests/test_basic.py` 全部通过（数据模型、瓶颈识别、浪费检测、工具函数）

## 2026-08-07 - V1.1 开发路线图制定

### 产出
- 新增 `docs/v1.1/开发路线图.md`：V1.1 产品范围（P0/P1/P2）、实现方式、
  架构调整方向、里程碑与 Loop 划分
- 新增 `docs/v1.1/开发日志模板.md` 与 `docs/v1.1/开发日志.md`：
  规范每个开发 Loop 的记录字段与成功/失败判定

### 关键决策
- V1.1 聚焦易用性与完整性：报告导出、切换事件、拖拽排序、方案扩展至 5 个、
  画布交互、快速配置向导、工程化基础
- 明确排除 Web 化（V2.0）与 AI 优化（V1.2）
- 架构保持 7 文件三层结构，新增 `version.py`、`reporting.py`，仿真引擎增加
  事件队列与 headless 模式

### 团队协作记录
- 已尝试调度产品/后端/前端/测试子 Agent，子 Agent 未产出文件；
  改由主控 Agent 完成四角色分析并落盘（详见 `docs/v1.1/开发日志.md` L0）

## 2026-08-07 - V1.1 开发启动（L0 工程化基础）

### 完成内容
- 新增 `src/version.py`（`__version__ = "1.1.0"`），窗口标题/帮助/关于统一引用
- `setup_logger()` 默认写入 `logs/app.log`；Tkinter 回调异常统一捕获并记录
- 新增 pytest 骨架：`conftest.py`、`test_models.py`、`test_utils.py`
- `requirements.txt` 新增 reportlab；新增 `requirements-dev.txt`

### 验证
- pytest：14 passed
- `tests/test_basic.py`：通过

### 说明
- 环境：Miniconda Python（已安装 pytest、pytest-cov、reportlab）
- 详细记录见 `docs/v1.1/开发日志.md` Loop L0b

## 2026-08-07 - L1 切换事件与 headless 仿真

### 完成内容
- `SimulationEngine` 新增事件队列与 `trigger_changeover()`（默认 45 分钟停机）
- 新增 `run_sync()` headless 模式与 `build_result()` 结果聚合
- `models.py` 新增 `SimulationResult`（KPI、报警、WIP 采样、切换事件）
- 新增 `tests/test_simulation.py`（5 个用例）

### 验证
- pytest：19 passed（L1 时点）

## 2026-08-07 - L2 报告导出（Excel / PDF）

### 完成内容
- 新增 `src/reporting.py`：export_excel / export_pdf / export_report
- Excel 报告 5 个 Sheet：KPI、工序产出、报警记录、WIP 采样、切换事件
- PDF 报告支持中文字体（Arial Unicode.ttf）
- GUI"分析 → 导出报告"接入实际导出
- 新增 `tests/test_reporting.py`（3 个用例）

### 验证
- pytest：22 passed

## 2026-08-07 - L3 画布交互（拖拽排序 / 双击 / 右键 / 选中联动）

### 完成内容
- 画布拖拽调整工序顺序（仿真运行中禁止，避免引擎状态不一致）
- 双击工序打开编辑对话框；右键菜单支持编辑/删除/触发切换
- 单击工序高亮并联动配置面板选中
- "仿真 → 触发切换"菜单入口

### 验证
- 语法检查通过；pytest：22 passed

## 2026-08-07 - L5 方案扩展至 5 个与持久化

### 完成内容
- `ScenarioManager.MAX_SCENARIOS`：3 → 5（满足 PRD 验收）
- 新增 JSON 持久化：save_to_file / load_from_file，自动保存到
  `configs/scenarios.json`，启动自动加载
- "分析 → 方案管理"对话框：查看与删除方案
- 新增 `tests/test_scenario.py`（4 个用例）

### 验证
- 语法检查通过；pytest：26 passed

## 2026-08-07 - L6 测试覆盖完善与覆盖率测量

### 完成内容
- 修复存量 Bug：
  - `SimulationEngine.stop()` 移除 SimPy 不存在的 `env.interrupt()`
  - `import_from_excel` 修复 CollaborationType 导入路径（支持根目录运行）
- 新增测试：线程模式仿真、切换事件路由、WIP 堵塞报警、空产线、
  方案对比/持久化边界、Excel 模板往返等

### 验证
- pytest：49 passed
- 覆盖率：models 94% / simulation 94% / scenario_manager 94% /
  reporting 88% / utils 71% / 总计 87%

## 2026-08-07 - L7 文档同步与全量回归

### 完成内容
- README 更新 V1.1 功能清单、开发/测试命令、FAQ、版本历史
- 项目成熟度分析更新（整体约 92%，P0/P1 全部落地）
- V1.1 路线图加入 Loop 进度表
- GUI 冒烟验证通过（主窗口、默认产线、画布、KPI）

### 验证
- pytest：49 passed；test_basic：通过
- GUI 冒烟：通过（授权环境执行）

## 2026-08-07 - L8 V1.1 全量验收与发布（v1.1.0）

### 验收结果
- P0（1-5）与 P1（1-4）全部完成并通过测试
- pytest：52 passed；test_basic：通过
- 覆盖率：models 94% / simulation 94% / scenario_manager 94% /
  reporting 88% / utils 71% / 总计 87%
- GUI 冒烟：通过（授权环境）
- 已打 tag `v1.1.0`

### 遗留
- P2：KPI 历史趋势图、平台打包、配置 Excel 导出
- V1.2：AI 自动优化、敏感性分析
- V2.0：Web 化（纯前端方案）

## 2026-08-07 - V1.1 深度诊断与验收

### 多 Agent 调度
- 尝试调度测试/UI/代码审查三个子 Agent（spawn + followup 两轮），
  子 Agent 均未收到任务描述，平台机制不可用；由主控代行并如实记录。

### 验证结果
- pytest：53 passed；test_basic：通过
- 端到端 21 项全部通过（JSON/Excel/仿真/切换/导出/方案 5/持久化/对比）
- GUI 深度冒烟：通过（菜单接线、画布回调、方案管理对话框）
- 覆盖率：核心模块 88%-94%

### 修复
- 同工序连续多个切换事件不再丢失（按顺序执行）
- README（2-5 个方案）、AGENTS.md（pytest 命令）、
  docs/方案对比功能设计.md（现状 5 个）文档一致性修复

### 报告
- 详见 `docs/v1.1/诊断验收报告.md`

## 2026-08-07 - V1.2.0 交互视觉专项立项与版本登记

### 决策
- V1.2.0 重新定义为交互体验与视觉体验专项（原 V1.2"AI 优化"顺延至 V1.3）
- 版本号登记：`src/version.py` → `1.2.0`

### 产出
- `docs/v1.2/开发路线图.md`：美学/前端/交互三角色诊断 + 提升内容 + 里程碑
- `docs/v1.2/开发日志.md`：U0 立项记录

### 多 Agent 调度
- 已尝试调度美学/前端设计/交互设计三个子 Agent，任务消息仍未送达，
  由主控代行三角色诊断并如实记录

### 下一步
- U1：设计令牌与主题（`src/theme.py`）

## 2026-08-07 - V1.2.0 U1 设计令牌与主题

### 完成内容
- 新增 `src/theme.py`：设计令牌（颜色/状态色/浅色/报警图标/间距/字体）
- `apply_theme()`：clam 主题 + PingFang SC/微软雅黑字体 + 组件样式
- `utils.py` 状态色/报警色统一引用令牌
- 主窗口启动应用主题

### 验证
- pytest：57 passed；GUI 冒烟通过

## 2026-08-07 - V1.2.0 U7 收尾与发布

### 完成内容
- 关键控件 Tooltip；apply_theme 增加 HiDPI 缩放
- 全量回归：pytest 58 passed，核心覆盖率 88%-94%
- 深度 GUI 冒烟通过（V1.2.0 全部视觉/交互特性）
- README / 成熟度分析 / 路线图 / 开发日志同步
- 已打 tag `v1.2.0`

### 遗留
- V1.3：AI 自动优化、敏感性分析
- V2.0：Web 化

## 2026-08-07 - V1.2.0 目视检查热修复（U7.1）

### 修复
- 班次配置对话框：补 `ShiftConfigDialog._btn_cancel`（此前点"取消"直接报错）
- 空画布鼠标移动：`find_closest` 空结果保护，不再 IndexError
- 新增对话框方法存在性回归测试

### 验证
- pytest：59 passed

## 2026-08-07 - V1.2.0 目视检查新增需求（U8）

### 完成内容
1. 报警面板新增"📋 复制报警"：一键复制全部报警文本到剪贴板
2. 供应链术语速查：新增 `src/glossary.py`，
   入口为配置面板"术语?"、帮助菜单"术语说明"、向导"术语速查"
3. WIP 逻辑校验：自动化测试确认 WIP 采样恒在 [0, 缓冲区容量]，
   满缓冲触发堵塞报警
4. 日成本说明：公式 = 总人数 × 时薪 × 班次时长（不扣休息）；
   班次配置界面展示公式，KPI 日成本卡片悬停提示

### 验证
- pytest：64 passed；GUI 冒烟（含剪贴板复制验证）通过

## 2026-08-07 - V1.2.0 定版与交付准备

### 定版
- 版本号统一 v1.2.0，tag `v1.2.0` 指向最终提交
- 新增仓库根 README（项目总览 + 压缩包快速开始）
- README / 成熟度分析 / V1.2 路线图 / 开发日志全部同步

### 压缩包
- 生成 `产线仿真-v1.2.0.zip`（含源码、启动器、README、文档）

### GitHub 交付
- gh CLI 已安装但 token 失效（MaxHou-infinity）
- 等待用户完成 `gh auth login` 授权后推送私有仓库

## 2026-08-07 - V1.2.0 GitHub 私有交付完成

- 已创建私有仓库：https://github.com/MaxHou-infinity/e-pod-line-simulator
- 已推送：main 分支 + v1.1.0 / v1.2.0 标签
- 本地压缩包：`E-Pod-Line-Simulator-v1.2.0.zip`（含 README 与双平台启动器）

## 2026-08-10 - V1.3.0 立项：烟油 / 尼古丁袋产品形态扩展

### 决策
- V1.3.0 定义为产品形态扩展：烟油（液体/批量/化工）与
  尼古丁袋（高速自动化/食品医药 GMP）
- 引入"生产类型模板"（assembly / liquid_filling / pouch_packaging）
- AI 优化顺延至 V1.4；Web 化仍属 V2.0
- 版本号登记：`src/version.py` → `1.3.0`

### 产出
- `docs/v1.3/需求与开发路线图.md`：需求规格、合规输入、验收标准、路线图
- `docs/v1.3/开发日志.md`：L0 立项记录

### 输入依据
- Nowledge：Delta 化工/GMP 人力逻辑、印尼 HPTL 多机构监管
  （BPOM 认证、SNI 9070 ≤16.6mg/袋、GMP 洁净、RPTKA 用工）

## 2026-08-10 - V1.3 范围修正：合规约束不影响产线排程

### 用户决策
- 合规约束（BPOM/SNI/GMP/RPTKA 等）**不影响产线排程**
- V1.3 不建模、不校验合规约束；合规属准入/上市层面，由外部流程管理

### 文档更新
- `docs/v1.3/需求与开发路线图.md`：3.5 改为"质量门 + 合规范围说明"，
  验收标准、M3-L6、风险、明确排除项同步修正
- `docs/v1.3/开发日志.md`：新增 L0.1 范围修正记录

## 2026-08-10 - V1.3 目标模式启动指令输出

- 新增 `docs/v1.3/目标模式启动指令.md`（可直接粘贴的自主开发提示词）
- 提示词已固化 V1.3 范围：P0/P1、合规约束不影响排程、L0→L8、
  验收标准与停止条件

## 2026-08-10 - V1.3 L0 生产类型模板基础

### 完成内容
- 新增 ProductionType（assembly/liquid_filling/pouch_packaging）
- 新增 JobRole（调香师/灌装操作员/QC/包装机手/清洗工/通用）
- Station 扩展：machine_takt、洁净区、质量门、清洗字段
- ProductionLine.production_type，JSON 序列化向后兼容
- 机台节拍产能公式：3600/节拍 × 机台数 × OEE

### 验证
- pytest：67 passed；GUI 冒烟通过（v1.3.0 + 旧配置兼容）

## 2026-08-10 - V1.3 L1 批量与配方数据模型

### 完成内容
- Recipe / Tank / Batch 数据模型（含序列化与状态枚举）
- ProductionLine 支持 recipes/batches/tanks/labor_config/cleanroom_limits/skill_matrix
- create_liquid_line / create_pouch_line 模板工厂
- Excel 模板支持生产类型/配方/罐/批次 Sheet 与导入

### 验证
- pytest：71 passed

## 2026-08-10 - V1.3 L2 向导与配置按类型切换

### 完成内容
- 向导第一步新增生产类型选择（组装/烟油灌装/尼古丁袋包装）
- 非组装类型自动生成对应模板产线
- 配置面板显示生产类型标签
- 工序对话框按类型显示机台节拍/清洗/质量门字段

### 验证
- pytest：71 passed；GUI 冒烟通过

## 2026-08-10 - V1.3 L3 批量过程仿真

### 完成内容
- 烟油批次过程：调配→陈化→灌装→QC→放行（SimPy 进程）
- 罐液位按批次量 × 收率更新
- run_sync 按生产类型分流（组装/袋装走工序，烟油走批次）
- 单批次仿真与手工计算误差 < 3%（测试验证）

### 验证
- pytest：72 passed

## 2026-08-10 - V1.3 L4 CIP/SIP 与机台节拍

### 完成内容
- 换配方触发 CIP/SIP 清洗停机并记录事件
- 并联工序按人数并发进程（修复产能低估，匹配节拍 × 工位模型）
- 尼古丁袋机台节拍使用 machine_takt 作为单件时间

### 验证
- pytest：74 passed

## 2026-08-07 - V1.2.0 U6 快捷键与脏状态

### 完成内容
- 快捷键：Ctrl+N 新建 / Ctrl+O 打开 / Ctrl+S 保存 / Ctrl+E 导入 /
  空格 开始/暂停/恢复（输入框聚焦时忽略）
- 未保存修改标记：新建/打开/向导/退出前确认，保存后清除
- 导出报告：仿真与导出过程显示状态栏进度
- 错误提示统一附日志路径 logs/app.log

### 验证
- pytest：57 passed；GUI 冒烟通过

## 2026-08-07 - V1.2.0 U5 KPI 卡片化与报警面板重构

### 完成内容
- KPI 仪表盘改为 6 张卡片（名称/数值/单位），使用表面色与边框令牌
- 报警记录带级别图标（⛔/⚠️/ℹ️），颜色统一引用设计令牌

### 验证
- pytest：57 passed；GUI 冒烟通过

## 2026-08-07 - V1.2.0 U4 拖拽落点与运行中交互禁用

### 完成内容
- 拖拽时最近目标工序显示主色虚线落点框
- 仿真运行中禁用：拖拽、速度选择、工序添加/编辑/删除
- 右键菜单运行中禁用编辑/删除，保留"触发切换"
- 停止仿真后恢复编辑能力；工序节点悬停显示手型光标

### 验证
- pytest：57 passed；GUI 冒烟通过

## 2026-08-07 - V1.2.0 U3 画布节点卡片化

### 完成内容
- 画布节点改为圆角卡片：浅色填充 + 边框 + 顶部实色状态条
- 瓶颈工序显示红色"!"徽标
- 选中节点主色描边（宽 3）
- 仿真状态更新同步卡片底色与状态条颜色

### 验证
- pytest：57 passed；GUI 冒烟通过

## 2026-08-07 - V1.2.0 U2 空状态与运行状态徽标

### 完成内容
- 空产线画布显示引导："暂无工序，请点击添加或使用快速配置向导"
- KPI 仪表盘新增运行状态徽标（●运行中 / ⏸已暂停 / ■已停止）
- 开始/暂停/停止按钮与徽标状态同步

### 验证
- pytest：57 passed；GUI 冒烟通过

## 2026-08-07 - L4 快速配置向导

### 完成内容
- 新增 `WizardDialog`：模板选择 → 数据来源（模板/Excel）→ 班次设置 → 完成摘要
- 模板：简单 3 工序 / 标准 5 工序 / 复杂 8 工序（真实参数）/ 空白
- 文件菜单新增"快速配置向导..."入口，支持完成后立即开始仿真

### 验证
- 语法检查通过；pytest：22 passed
