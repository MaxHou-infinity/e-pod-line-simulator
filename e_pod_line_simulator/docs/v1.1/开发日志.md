# V1.1 开发日志

> 记录 V1.1 每个开发 Loop 的成功与失败。模板见 `开发日志模板.md`。

## Loop L0 - V1.1 路线图制定

### 基本信息

- 日期：2026-08-07
- 负责角色：主控 Agent（产品 / 后端 / 前端 / 测试四角色分析）
- 关联功能：规划

### 1. 目标与验收标准

- 目标：明确 V1.1 产品范围、实现方式、架构调整方向与 Loop 记录规范。
- 验收标准：
  - [x] 输出开发路线图文档
  - [x] 输出 Loop 日志模板

### 2. 计划改动

| 文件 | 改动内容 |
|------|----------|
| docs/v1.1/开发路线图.md | 新增：范围、实现方案、架构方向、里程碑 |
| docs/v1.1/开发日志模板.md | 新增：Loop 记录模板 |
| docs/v1.1/开发日志.md | 新增：本条目 |

### 3. 实际执行

- 已完成：检索历史上下文、盘点 V1.0 现状、完成四角色分析、落盘三份文档。
- 未完成：子 Agent 调度（产品/后端/前端/测试）未产出文件，改由主控 Agent 汇总。

### 4. 验证结果

- 文档已创建并通过 `ls` 确认；
- 未运行代码测试（本 Loop 仅规划，无代码改动）。

### 5. 结果判定

- [x] ✅ 成功（规划产出）
- [ ] ❌ 失败
- [ ] ⚠️ 部分成功

### 6. 失败根因与调整决策

- 根因：子 Agent 未按指令落盘文件（疑似 apply_patch 无法自动创建父目录及子 Agent 执行异常）。
- 调整决策：后续 Loop 先由主控创建目录再调度；重要交付物由主控验收文件存在性。

### 7. 文档同步

- [x] 路线图已新增
- [x] 日志模板已新增
- [ ] README（待 M1 阶段补充 V1.1 索引）
- [ ] 项目成熟度分析（待功能落地后更新）

### 8. 下一 Loop 输入

- 遗留问题：无代码改动。
- 下个 Loop 目标：L0 工程化基础（版本号、日志、pytest 骨架）。

## Loop L0b - 工程化基础

### 基本信息

- 日期：2026-08-07
- 负责角色：主控（后端 / 测试）
- 关联功能：P0-5、P1-3、P1-4

### 1. 目标与验收标准

- 目标：建立 V1.1 工程化基础（版本号统一、日志落盘、全局异常捕获、pytest 骨架）。
- 验收标准：
  - [x] `src/version.py` 定义 `__version__ = "1.1.0"`，窗口标题/帮助/关于引用
  - [x] `setup_logger()` 默认同时写入控制台和 `logs/app.log`
  - [x] Tkinter 回调异常统一记录日志并弹窗
  - [x] pytest 骨架可用，`requirements-dev.txt` 提供测试依赖

### 2. 计划改动

| 文件 | 改动内容 |
|------|----------|
| src/version.py | 新增版本号入口 |
| src/gui_main.py | 窗口标题引用版本号；新增全局异常捕获 |
| src/utils.py | setup_logger 默认写入日志文件 |
| tests/ | 新增 conftest.py、test_models.py、test_utils.py |
| requirements*.txt | 新增 reportlab、pytest、pytest-cov |

### 3. 实际执行

- 已完成：全部计划改动；安装 pytest/pytest-cov（Miniconda）。
- 未完成：无。

### 4. 验证结果

```bash
/opt/homebrew/Caskroom/miniconda/base/bin/python3 -m pytest e_pod_line_simulator/tests -q
python tests/test_basic.py
```

- pytest：14 passed
- 旧脚本测试：全部通过

### 5. 结果判定

- [x] ✅ 成功

### 6. 失败根因与调整决策

- 根因：`test_basic.py` 的函数带参数会被 pytest 当作 fixture 收集。
- 调整决策：在 `conftest.py` 中 `collect_ignore = ["test_basic.py"]`，保留其独立脚本入口。

### 7. 文档同步

- [ ] README（L7 统一更新）
- [ ] 项目成熟度分析（L7 统一更新）
- [x] logs.md 已追加

### 8. 下一 Loop 输入

- 遗留问题：无。
- 下个 Loop 目标：L1 切换事件与 headless 仿真。

## Loop L1 - 切换事件与 headless 仿真

### 基本信息

- 日期：2026-08-07
- 负责角色：主控（后端）
- 关联功能：P0-2

### 1. 目标与验收标准

- 目标：仿真引擎支持切换事件注入，并提供 headless 同步运行与结果聚合。
- 验收标准：
  - [x] `trigger_changeover(station_id, minutes)` 可触发指定工序停机
  - [x] 切换期间工序进入 changeover 状态，上下游按 WIP 机制受影响
  - [x] `run_sync()` 不依赖 GUI 返回 `SimulationResult`
  - [x] WIP 时间序列采样与切换事件进入结果对象

### 2. 计划改动

| 文件 | 改动内容 |
|------|----------|
| src/models.py | 新增 `SimulationResult` 聚合对象 |
| src/simulation.py | 事件队列、trigger_changeover、run_sync、build_result、WIP 采样 |
| tests/test_simulation.py | 新增 5 个引擎测试 |

### 3. 实际执行

- 已完成：全部计划改动。
- 未完成：无。

### 4. 验证结果

```bash
/opt/homebrew/Caskroom/miniconda/base/bin/python3 -m pytest e_pod_line_simulator/tests -q
```

- pytest：L1 完成时 19 passed（含 reporting 后为 22 passed）

### 5. 结果判定

- [x] ✅ 成功

### 6. 失败根因与调整决策

- 根因 1：`except queue.Empty` 未导入 queue 模块 → 改为 `from queue import Queue, Empty`。
- 根因 2：run 开头清空事件队列导致运行前注入的切换事件丢失 → 移除清空逻辑
  （GUI 每次新仿真都创建新引擎，无残留事件问题）。
- 根因 3：WIP 采样只在报警时记录 → 改为每个监控周期都记录。

### 7. 文档同步

- [ ] README（L7 统一更新）
- [ ] 项目成熟度分析（L7 统一更新）
- [x] logs.md 已追加

### 8. 下一 Loop 输入

- 遗留问题：GUI 触发切换入口在 L4 接入。
- 下个 Loop 目标：L2 报告导出。

## Loop L2 - 报告导出（Excel / PDF）

### 基本信息

- 日期：2026-08-07
- 负责角色：主控（后端）
- 关联功能：P0-1

### 1. 目标与验收标准

- 目标：仿真结果可导出为 Excel 与 PDF 报告。
- 验收标准：
  - [x] Excel 报告含 KPI、工序产出、报警记录、WIP 采样、切换事件 5 个 Sheet
  - [x] PDF 报告含标题、KPI 表、报警表，中文字体可用
  - [x] GUI"导出报告"菜单可导出当前配置的仿真结果

### 2. 计划改动

| 文件 | 改动内容 |
|------|----------|
| src/reporting.py | 新增 export_excel / export_pdf / export_report |
| src/gui_main.py | 导出报告菜单接入实际导出 |
| tests/test_reporting.py | 新增 3 个导出测试 |

### 3. 实际执行

- 已完成：全部计划改动；中文字体使用 Arial Unicode.ttf。
- 未完成：GUI 菜单需最终冒烟验证（L8 统一执行）。

### 4. 验证结果

```bash
/opt/homebrew/Caskroom/miniconda/base/bin/python3 -m pytest e_pod_line_simulator/tests -q
```

- pytest：22 passed（含 test_reporting 3 个用例）

### 5. 结果判定

- [x] ✅ 成功

### 6. 失败根因与调整决策

- 根因：无。
- 调整决策：PDF 字体优先使用 TTF 格式（Arial Unicode），TTC 作为备选。

### 7. 文档同步

- [ ] README（L7 统一更新）
- [ ] 项目成熟度分析（L7 统一更新）
- [x] logs.md 已追加

### 8. 下一 Loop 输入

- 遗留问题：无。
- 下个 Loop 目标：L3 工序拖拽排序。

## Loop L3 - 画布交互：拖拽排序与工序菜单（P0-3 + P1-1 合并实施）

### 基本信息

- 日期：2026-08-07
- 负责角色：主控（前端）
- 关联功能：P0-3、P1-1

### 1. 目标与验收标准

- 目标：画布支持拖拽排序、双击编辑、右键菜单、单击选中联动。
- 验收标准：
  - [x] 拖拽工序节点可调整顺序，画布与配置面板同步
  - [x] 双击工序打开编辑对话框
  - [x] 右键菜单支持编辑/删除/触发切换
  - [x] 单击工序高亮并联动配置面板选中
  - [x] 仿真运行中禁止拖拽排序（避免引擎状态不一致）

### 2. 计划改动

| 文件 | 改动内容 |
|------|----------|
| src/gui_canvas.py | 拖拽事件、双击/右键/单击回调、station_positions 记录 |
| src/gui_main.py | 重排/编辑/删除/切换菜单回调，仿真菜单增加"触发切换" |
| src/gui_panels.py | ConfigPanel.select_station() 联动选中 |

### 3. 实际执行

- 已完成：全部计划改动。
- 未完成：无（GUI 冒烟验证统一在 L8 执行）。

### 4. 验证结果

```bash
python -m py_compile src/gui_canvas.py src/gui_main.py src/gui_panels.py
pytest e_pod_line_simulator/tests -q
```

- 语法检查通过；pytest 22 passed。

### 5. 结果判定

- [x] ✅ 成功

### 6. 失败根因与调整决策

- 根因：无（引擎切换入口原计划 L4，实际随右键菜单一并完成）。
- 调整决策：将 P0-3 与 P1-1 合并为同一 Loop 交付，后续 L4 直接进入向导。

### 7. 文档同步

- [ ] README（L7 统一更新）
- [ ] 项目成熟度分析（L7 统一更新）
- [x] logs.md 已追加

### 8. 下一 Loop 输入

- 遗留问题：无。
- 下个 Loop 目标：L4 快速配置向导。

## Loop L4 - 快速配置向导（P1-2）

### 基本信息

- 日期：2026-08-07
- 负责角色：主控（前端）
- 关联功能：P1-2

### 1. 目标与验收标准

- 目标：新手可通过分步向导 5 分钟内完成产线配置。
- 验收标准：
  - [x] 向导含模板选择（简单/标准/复杂/空白）
  - [x] 支持 Excel 导入替换模板数据
  - [x] 支持班次设置，完成页展示摘要
  - [x] 可选择配置完成后立即开始仿真
  - [x] 可随时取消/跳过

### 2. 计划改动

| 文件 | 改动内容 |
|------|----------|
| src/gui_panels.py | 新增 WizardDialog（4 步向导） |
| src/gui_main.py | 文件菜单新增"快速配置向导"入口 |

### 3. 实际执行

- 已完成：全部计划改动。
- 未完成：无（GUI 冒烟验证统一在 L8）。

### 4. 验证结果

```bash
python -m py_compile src/gui_panels.py src/gui_main.py
pytest e_pod_line_simulator/tests -q
```

- 语法检查通过；pytest 22 passed。

### 5. 结果判定

- [x] ✅ 成功

### 6. 失败根因与调整决策

- 根因：无。
- 调整决策：复杂模板直接采用真实产线参数（镭雕→异物检查），便于直接产出可用配置。

### 7. 文档同步

- [ ] README（L7 统一更新）
- [ ] 项目成熟度分析（L7 统一更新）
- [x] logs.md 已追加

### 8. 下一 Loop 输入

- 遗留问题：无。
- 下个 Loop 目标：L5 方案扩展至 5 个与持久化。

## Loop L5 - 方案扩展至 5 个与持久化（P0-4）

### 基本信息

- 日期：2026-08-07
- 负责角色：主控（后端 / 前端）
- 关联功能：P0-4

### 1. 目标与验收标准

- 目标：方案数量满足 PRD 验收（至少 5 个），并支持持久化与可视化管理。
- 验收标准：
  - [x] `MAX_SCENARIOS = 5`，第 6 个方案被拒绝并提示上限
  - [x] 保存/删除方案自动持久化到 `configs/scenarios.json`
  - [x] 启动时自动加载已保存方案
  - [x] "分析 → 方案管理"支持查看与删除

### 2. 计划改动

| 文件 | 改动内容 |
|------|----------|
| src/scenario_manager.py | MAX_SCENARIOS 5；save_to_file / load_from_file / set_storage_path |
| src/gui_panels.py | 新增 ScenarioManageDialog |
| src/gui_main.py | 自动加载方案、分析菜单新增"方案管理" |
| tests/test_scenario.py | 新增 4 个方案管理测试 |

### 3. 实际执行

- 已完成：全部计划改动。
- 未完成：无。

### 4. 验证结果

```bash
python -m py_compile src/scenario_manager.py src/gui_panels.py src/gui_main.py
pytest e_pod_line_simulator/tests -q
```

- 语法检查通过；pytest 26 passed。

### 5. 结果判定

- [x] ✅ 成功

### 6. 失败根因与调整决策

- 根因：无。
- 调整决策：持久化采用自动落盘（静默失败），避免存储问题阻塞主流程。

### 7. 文档同步

- [ ] README（L7 统一更新）
- [ ] 项目成熟度分析（L7 统一更新）
- [x] logs.md 已追加

### 8. 下一 Loop 输入

- 遗留问题：无。
- 下个 Loop 目标：L6 测试覆盖完善与覆盖率测量。

## Loop L6 - 测试覆盖完善与覆盖率测量（P1-4）

### 基本信息

- 日期：2026-08-07
- 负责角色：主控（测试）
- 关联功能：P1-4

### 1. 目标与验收标准

- 目标：核心模块行覆盖率 ≥80%，V1.0 回归项（含 Excel 导入）进入自动化测试。
- 验收标准：
  - [x] models / simulation / scenario_manager / reporting 覆盖率 ≥80%
  - [x] pytest 全绿（49 passed）
  - [x] 修复 Stop 仿真的环境中断错误
  - [x] 修复 Excel 导入在非 src 目录运行失败的问题

### 2. 计划改动

| 文件 | 改动内容 |
|------|----------|
| src/simulation.py | 修复 stop()：移除不存在的 env.interrupt() |
| src/utils.py | 修复 import_from_excel 的 CollaborationType 导入路径 |
| tests/test_simulation.py | 线程模式、回调、堵塞报警、空产线等 10 个用例 |
| tests/test_scenario.py | 对比/边界/持久化等 10 个用例 |
| tests/test_utils.py | Excel 模板往返、ROI、日志等 7 个用例 |

### 3. 实际执行

- 已完成：全部计划改动。
- 未完成：无。

### 4. 验证结果

```bash
pytest tests --cov=src.models --cov=src.simulation \
  --cov=src.scenario_manager --cov=src.reporting --cov=src.utils \
  --cov-report=term-missing -q
```

- pytest：49 passed
- 覆盖率：models 94% / simulation 94% / scenario_manager 94% / reporting 88% /
  utils 71% / 总计 87%

### 5. 结果判定

- [x] ✅ 成功

### 6. 失败根因与调整决策

- 根因 1：`SimulationEngine.stop()` 调用了 SimPy Environment 不存在的
  `interrupt()` 方法（GUI 中因 try/except 被隐藏）→ 改为仅置 `is_running=False`，
  线程按小步长自然退出。
- 根因 2：`import_from_excel` 使用 `from models import CollaborationType`，
  从项目根目录运行时失败 → 改为优先 `src.models`，兼容旧路径。
- 根因 3：方案对比推荐测试中"减人"不必然降低单颗成本 → 测试改为同时提升
  OEE/效率构造严格更优方案。

### 7. 文档同步

- [ ] README（L7 统一更新）
- [ ] 项目成熟度分析（L7 统一更新）
- [x] logs.md 已追加

### 8. 下一 Loop 输入

- 遗留问题：无。
- 下个 Loop 目标：L7 文档同步与全量回归。
