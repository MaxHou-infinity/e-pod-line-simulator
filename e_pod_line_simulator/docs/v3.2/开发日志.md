# V3.2 开发日志

> 追踪每个开发 Loop 的目标、改动、验证结果与遗留事项。
> 模板沿用 `docs/v1.1/开发日志模板.md`。

## Loop L1 - 版本登记 v3.2.0 与开发日志建立

### 基本信息

- 日期：2026-08-11
- 负责角色：主控
- 关联功能：版本管理 / 文档

### 1. 目标与验收标准

- 目标：完成 V3.2 版本登记并建立开发 Loop 追踪文档。
- 验收标准：
  - [x] `src/version.py` 升至 3.2.0
  - [x] 路线图文档从 v4.0 迁移至 `docs/v3.2/`
  - [x] README 徽章 / 路线图 / 文档导航同步
  - [x] 本开发日志建立
  - [x] pytest 全绿（95 passed）

### 2. 计划改动

| 文件 | 改动内容 |
|------|----------|
| src/version.py | 3.1.0 → 3.2.0 |
| docs/v3.2/需求与开发路线图.md | v4.0 文档迁移并改版本号 |
| docs/v3.2/开发日志.md | 本文件（Loop 追踪） |
| README.md | 徽章/路线图/导航 |
| CHANGELOG.md | Unreleased 增加 V3.2 |
| logs.md | 立项记录修正为 V3.2 |

### 3. 实际执行

- 已完成：全部计划改动。
- 未完成：无。

### 4. 验证结果

- pytest：95 passed

### 5. 结果判定

- [x] ✅ 成功

### 6. 失败根因与调整决策

- 根因：无。

### 7. 文档同步

- [x] README / CHANGELOG / logs.md 已更新

### 8. 下一 Loop 输入

- 遗留问题：无。
- 下个 Loop 目标：L2 烟油罐容约束（满罐等待/报警，禁止静默丢弃）。

## Loop L2 - 烟油罐容约束

### 基本信息

- 日期：2026-08-11
- 负责角色：主控
- 关联功能：烟油批量仿真

### 1. 目标与验收标准

- 目标：罐容不足时批次等待并报警，禁止静默丢弃；成品罐按灌装线
  产能持续消耗。
- 验收标准：
  - [x] Tank 提供 `available_capacity` / `add_liquid` / `withdraw`
  - [x] 批次按可用罐容分批注入，容量不足时等待并触发
    `tank_full` 报警（每批次一次）
  - [x] 新增罐消耗进程：按首工序（灌装）产能持续从成品罐取液
  - [x] 全量 pytest 全绿（97 passed）

### 2. 计划改动

| 文件 | 改动内容 |
|------|----------|
| src/models.py | Tank 容量相关方法 |
| src/simulation.py | 分批注入/等待报警/罐消耗进程 |
| tests/test_simulation.py | 新增 2 用例，更新 1 处断言 |

### 3. 实际执行

- 已完成：全部计划改动。
- 未完成：无。

### 4. 验证结果

- pytest：97 passed（含 2 个新用例）

### 5. 结果判定

- [x] ✅ 成功

### 6. 失败根因与调整决策

- 初版"等满整批罐容再注入"会导致放行时间不可控，改为
  "按可用罐容分批注入 + 等待"，更贴近真实灌装且可复现。
- 原测试断言"罐液位常驻 475L"随消耗语义更新为"≤ 批次量 × 收率"。

### 7. 文档同步

- [x] 本日志已记录；logs.md 待定版时统一追加

### 8. 下一 Loop 输入

- 遗留问题：无。
- 下个 Loop 目标：L3 批次排产序列与周期性 CIP。

## Loop L3 - 批次排产序列与周期性 CIP

### 基本信息

- 日期：2026-08-11
- 负责角色：主控
- 关联功能：烟油批量排产

### 1. 目标与验收标准

- 目标：批次按顺序执行（前一批放行后下一批开始），换配方触发 CIP；
  支持按批次数/运行小时数触发周期性 CIP。
- 验收标准：
  - [x] `ProductionLine` 新增 `cip_interval_batches` /
    `cip_interval_hours`（JSON 可持久化）
  - [x] 新增 `_batch_sequence_process`：批次串行执行
  - [x] 清洗事件带 `reason`（recipe_change / periodic）
  - [x] 配方切换清洗与周期清洗均重置周期计时
  - [x] 全量 pytest 全绿（99 passed）

### 2. 计划改动

| 文件 | 改动内容 |
|------|----------|
| src/models.py | CIP 间隔字段 + JSON 序列化 |
| src/simulation.py | 批次序列进程 / 周期清洗 / 清洗事件原因 |
| tests/test_simulation.py | 新增 2 用例 |

### 3. 实际执行

- 已完成：全部计划改动。
- 未完成：陈化罐组并行（延后至批次调度增强 Loop）。

### 4. 验证结果

- pytest：99 passed（含 2 个新用例）

### 5. 结果判定

- [x] ✅ 成功

### 6. 失败根因与调整决策

- 根因：无。
- 调整决策：并行陈化涉及罐组调度复杂度较高，为保证本 Loop 可验收、
  不引入破坏性影响，先落地串行排产 + 周期 CIP，并行陈化另行立项。

### 7. 文档同步

- [x] 本日志已记录

### 8. 下一 Loop 输入

- 遗留问题：陈化罐组并行（P1）。
- 下个 Loop 目标：L4 原料与库存（到货计划/投料消耗/缺料阻塞）。

## Loop L4 - 原料与库存

### 基本信息

- 日期：2026-08-11
- 负责角色：主控
- 关联功能：烟油批量排产

### 1. 目标与验收标准

- 目标：原料到货计划、投料消耗与缺料阻塞。
- 验收标准：
  - [x] 新增 `Material` / `MaterialArrival` 模型与 JSON 持久化
  - [x] 批次投料按配方原料 × 批次量/基准量 消耗库存
  - [x] 库存不足时批次阻塞并触发 `material_shortage` 报警
  - [x] 到货进程按计划时间补充库存
  - [x] 未配置原料的旧产线行为不变（向后兼容）
  - [x] 报告新增"原料事件"Sheet；结果含库存快照
  - [x] 全量 pytest 全绿（100 passed）

### 2. 计划改动

| 文件 | 改动内容 |
|------|----------|
| src/models.py | Material/MaterialArrival/库存字段/结果字段 |
| src/simulation.py | 到货进程/投料等待/缺料报警/库存快照 |
| src/reporting.py | 原料事件 Sheet |
| tests/test_simulation.py | 新增 1 用例 |

### 3. 实际执行

- 已完成：全部计划改动。
- 未完成：无。

### 4. 验证结果

- pytest：100 passed（含 1 个新用例）

### 5. 结果判定

- [x] ✅ 成功

### 6. 失败根因与调整决策

- 初版对所有烟油批次强制投料校验，导致无原料配置的旧测试死等；
  调整为"仅当配置原料或库存时启用投料约束"，保持向后兼容。

### 7. 文档同步

- [x] 本日志已记录

### 8. 下一 Loop 输入

- 遗留问题：无。
- 下个 Loop 目标：L5 质量门放行/隔离（未放行批次不计入可发运产出）。

## Loop L5 - 质量门放行/隔离

### 基本信息

- 日期：2026-08-11
- 负责角色：主控
- 关联功能：烟油质量门

### 1. 目标与验收标准

- 目标：未通过质量门的批次隔离，不计入可发运产出。
- 验收标准：
  - [x] `BatchStatus.REJECTED` 新增
  - [x] 质量门未通过 → 批次隔离、跳过罐注入、触发
    `batch_rejected` 报警
  - [x] KPI 新增 `shippable_quantity` / `rejected_batches`
  - [x] 报告 KPI 新增"可发运产出/隔离批次"
  - [x] 全量 pytest 全绿（101 passed）

### 2. 计划改动

| 文件 | 改动内容 |
|------|----------|
| src/models.py | BatchStatus.REJECTED |
| src/simulation.py | 隔离逻辑/KPI |
| src/reporting.py | KPI 行扩展 |
| tests/test_simulation.py | 新增 1 用例 |

### 3. 实际执行

- 已完成：全部计划改动。
- 未完成：无。

### 4. 验证结果

- pytest：101 passed（含 1 个新用例）

### 5. 结果判定

- [x] ✅ 成功

### 6. 失败根因与调整决策

- 根因：无。
- 调整决策：隔离批次仍保留 `batch_results` 记录（便于追溯），
  仅 `yield_l=0`、不计入可发运产出。

### 7. 文档同步

- [x] 本日志已记录

### 8. 下一 Loop 输入

- 遗留问题：无。
- 下个 Loop 目标：L6 人力规划数据模型（目标产量/班次/成本/爬坡/招聘）。

## Loop L6 - 人力规划数据模型与核心计算

### 基本信息

- 日期：2026-08-11
- 负责角色：主控
- 关联功能：HRBP/HR 人力规划

### 1. 目标与验收标准

- 目标：建立纯计算层，支持目标产量反推人力、排班、成本、
  招聘缺口与爬坡达产。
- 验收标准：
  - [x] 新增 `src/hr_planning.py`（ShiftPlan / LaborCostConfig /
    LearningCurveConfig）
  - [x] 按工序/工种反推人力（并联按单人工位产能、协同保持团队）
  - [x] 成本测算（基本工资/加班/社保/缺勤覆盖/招聘/培训）
  - [x] 按周招聘缺口时间线
  - [x] 新员工爬坡达产天数
  - [x] 全量 pytest 全绿（107 passed）

### 2. 计划改动

| 文件 | 改动内容 |
|------|----------|
| src/hr_planning.py | 新增计算模块 |
| tests/test_hr_planning.py | 新增 6 用例 |

### 3. 实际执行

- 已完成：全部计划改动。
- 未完成：无。

### 4. 验证结果

- pytest：107 passed（含 6 个新用例）

### 5. 结果判定

- [x] ✅ 成功

### 6. 失败根因与调整决策

- 根因：无。
- 调整决策：爬坡达产采用"总人数为代理 + 新员工效率曲线"的近似模型，
  精度满足规划估算用途，正式投产前需现场验证。

### 7. 文档同步

- [x] 本日志已记录

### 8. 下一 Loop 输入

- 遗留问题：无。
- 下个 Loop 目标：L7 HR 报告导出（Excel 人力规划 Sheet + PDF 摘要）。

## Loop L7 - HR 报告导出

### 基本信息

- 日期：2026-08-11
- 负责角色：主控
- 关联功能：HR 报告

### 1. 目标与验收标准

- 目标：人力规划结果可导出为 Excel 与 PDF。
- 验收标准：
  - [x] `export_hr_report`：Excel 含人力需求-工序/工种、成本、招聘缺口
  - [x] `export_hr_pdf`：PDF 摘要（人力/成本/达产预测）
  - [x] 全量 pytest 全绿（108 passed）

### 2. 计划改动

| 文件 | 改动内容 |
|------|----------|
| src/reporting.py | 新增两个导出函数 |
| tests/test_reporting.py | 新增 1 用例 |

### 3. 实际执行

- 已完成：全部计划改动。
- 未完成：无。

### 4. 验证结果

- pytest：108 passed（含 1 个新用例）

### 5. 结果判定

- [x] ✅ 成功

### 6. 失败根因与调整决策

- 根因：无。

### 7. 文档同步

- [x] 本日志已记录

### 8. 下一 Loop 输入

- 遗留问题：无。
- 下个 Loop 目标：L8 人力规划向导 GUI（菜单入口 + 结果展示 + 导出）。

## Loop L8 - 人力规划向导 GUI

### 基本信息

- 日期：2026-08-11
- 负责角色：主控（前端）
- 关联功能：HRBP/HR 人力规划

### 1. 目标与验收标准

- 目标：提供可视化的「人力规划」入口，支持计算、展示与导出。
- 验收标准：
  - [x] 新增 `HrPlanningDialog`（目标产量/班次/成本/爬坡/招聘输入）
  - [x] 结果展示：工种人数、成本、招聘缺口、达产预测
  - [x] 导出 Excel/PDF
  - [x] 菜单「分析 → 人力规划...」接入
  - [x] 全量 pytest 全绿（109 passed）+ 语法检查通过

### 2. 计划改动

| 文件 | 改动内容 |
|------|----------|
| src/gui_panels.py | HrPlanningDialog |
| src/gui_main.py | 菜单与入口方法 |
| tests/test_gui_logic.py | 新增方法存在性用例 |

### 3. 实际执行

- 已完成：全部计划改动。
- 未完成：无。

### 4. 验证结果

- pytest：109 passed（含 1 个新用例）；py_compile 通过

### 5. 结果判定

- [x] ✅ 成功

### 6. 失败根因与调整决策

- 根因：无。

### 7. 文档同步

- [x] 本日志已记录

### 8. 下一 Loop 输入

- 遗留问题：无。
- 下个 Loop 目标：L9 组装 BOM/组件缺料/返修回路（P1）。

## Loop L9 - 组装 BOM / 缺料 / 返修回路

### 基本信息

- 日期：2026-08-11
- 负责角色：主控
- 关联功能：烟弹组装深化

### 1. 目标与验收标准

- 目标：装配工序支持 BOM 组件消耗、缺料阻塞与返工回路。
- 验收标准：
  - [x] `Station.bom`（组件名 → 单件用量），JSON 自动兼容
  - [x] 工序级 BOM 投料：缺料等待 + `material_shortage` 报警
  - [x] 返工回路：缺陷件非阻塞回写本工序缓冲区（避免满缓冲死锁）
  - [x] 原料到货进程对所有生产类型生效（原仅烟油）
  - [x] 全量 pytest 全绿（111 passed）

### 2. 计划改动

| 文件 | 改动内容 |
|------|----------|
| src/models.py | Station.bom |
| src/simulation.py | 工序投料/返工回写/到货进程全局化 |
| tests/test_simulation.py | 新增 2 用例 |

### 3. 实际执行

- 已完成：全部计划改动。
- 未完成：无。

### 4. 验证结果

- pytest：111 passed（含 2 个新用例）

### 5. 结果判定

- [x] ✅ 成功

### 6. 失败根因与调整决策

- 返工回写初版使用阻塞 `put`，缓冲区满时造成死锁；
  改为非阻塞 `items.append` 回写，兼顾顺序近似与无死锁。
- 原料到货进程原先仅烟油启动，组装 BOM 场景下不生效；已全局化。

### 7. 文档同步

- [x] 本日志已记录

### 8. 下一 Loop 输入

- 遗留问题：无。
- 下个 Loop 目标：L10 尼古丁袋 OEE 三要素分解与换型矩阵（P1）。

## Loop L10 - 尼古丁袋 OEE 分解与换型矩阵

### 基本信息

- 日期：2026-08-11
- 负责角色：主控
- 关联功能：尼古丁袋高速包装

### 1. 目标与验收标准

- 目标：工序指标增加 OEE 三要素分解；换型矩阵参与配方切换计时。
- 验收标准：
  - [x] station_metrics 新增 oee_availability / oee_performance /
    oee_quality / oee_total（A×P×Q）
  - [x] `ProductionLine.changeover_matrix`（配方 A→B 时长，分钟），
    JSON 持久化
  - [x] 配方切换优先使用换型矩阵时长，其次配方清洗时长
  - [x] 报告"工序指标"Sheet 增加 OEE 列
  - [x] 全量 pytest 全绿（113 passed）

### 2. 计划改动

| 文件 | 改动内容 |
|------|----------|
| src/models.py | changeover_matrix |
| src/simulation.py | OEE 分解 / 换型矩阵计时 |
| src/reporting.py | OEE 列 |
| tests/test_simulation.py | 新增 2 用例 |

### 3. 实际执行

- 已完成：全部计划改动。
- 未完成：包装材料供料由 L9 BOM 能力覆盖（模板未内置库存）。

### 4. 验证结果

- pytest：113 passed（含 2 个新用例）

### 5. 结果判定

- [x] ✅ 成功

### 6. 失败根因与调整决策

- 根因：无。
- 调整决策：OEE 性能率以"工序有效产能"为理想速率（含 OEE/效率折减），
  便于与整线 KPI 口径一致。

### 7. 文档同步

- [x] 本日志已记录

### 8. 下一 Loop 输入

- 遗留问题：无。
- 下个 Loop 目标：L11 KPI 历史趋势（跨运行记录/趋势表/图）。

## Loop L11 - KPI 历史趋势

### 基本信息

- 日期：2026-08-11
- 负责角色：主控
- 关联功能：评估能力

### 1. 目标与验收标准

- 目标：跨运行记录 KPI，提供趋势表与折线图。
- 验收标准：
  - [x] 新增 `src/history.py`（快照/持久化/序列，上限 200 条）
  - [x] 停止仿真与「记录当前产线」自动写入历史
  - [x] 新增 `HistoryDialog`（表格 + KPI 折线图）
  - [x] 菜单「分析 → KPI 历史趋势...」
  - [x] 全量 pytest 全绿（117 passed）

### 2. 计划改动

| 文件 | 改动内容 |
|------|----------|
| src/history.py | 新增历史模块 |
| src/gui_panels.py | HistoryDialog |
| src/gui_main.py | 菜单/停止时自动记录 |
| tests/ | test_history.py（3 用例）+ GUI 方法检查 |

### 3. 实际执行

- 已完成：全部计划改动。
- 未完成：无。

### 4. 验证结果

- pytest：117 passed（含 4 个新用例）

### 5. 结果判定

- [x] ✅ 成功

### 6. 失败根因与调整决策

- 根因：无。
- 调整决策：历史记录上限 200 条，防止配置文件无限膨胀。

### 7. 文档同步

- [x] 本日志已记录

### 8. 下一 Loop 输入

- 遗留问题：无。
- 下个 Loop 目标：L12 批量试算（参数网格自动仿真对比）。

## Loop L12 - 批量试算

### 基本信息

- 日期：2026-08-11
- 负责角色：主控
- 关联功能：评估能力

### 1. 目标与验收标准

- 目标：同一参数多取值批量仿真对比。
- 验收标准：
  - [x] `run_sweep`：worker_count / machine_takt / oee / shift_hours
  - [x] 新增 `SweepDialog`（参数/工序/取值/时长 + 结果表）
  - [x] 菜单「分析 → 批量试算...」
  - [x] 全量 pytest 全绿（120 passed）

### 2. 计划改动

| 文件 | 改动内容 |
|------|----------|
| src/sensitivity.py | run_sweep |
| src/gui_panels.py | SweepDialog |
| src/gui_main.py | 菜单接入 |
| tests/test_sensitivity.py | 新增 2 用例 |

### 3. 实际执行

- 已完成：全部计划改动。
- 未完成：无。

### 4. 验证结果

- pytest：120 passed（含 3 个新用例）

### 5. 结果判定

- [x] ✅ 成功

### 6. 失败根因与调整决策

- 根因：无。

### 7. 文档同步

- [x] 本日志已记录

### 8. 下一 Loop 输入

- 遗留问题：无。
- 下个 Loop 目标：L13 敏感性扩展（多岗位加减人/自动化替代/原料价格）。

## Loop L13 - 敏感性扩展

### 基本信息

- 日期：2026-08-11
- 负责角色：主控
- 关联功能：评估能力

### 1. 目标与验收标准

- 目标：敏感性扩展至多岗位加减人、自动化替代与原料价格。
- 验收标准：
  - [x] 新增"瓶颈+2人 / 瓶颈-1人 / 自动化替代10% / 增加1台机台"
  - [x] `Material.unit_cost` 与 KPI `material_cost`（按消耗量计价）
  - [x] 原料价格 ±10% 试算（含 delta_material_cost）
  - [x] 全量 pytest 全绿（122 passed）

### 2. 计划改动

| 文件 | 改动内容 |
|------|----------|
| src/models.py | Material.unit_cost |
| src/simulation.py | material_cost KPI |
| src/sensitivity.py | 扩展变体（lambda 改为作用于产线克隆） |
| tests/test_sensitivity.py | 新增 2 用例 |

### 3. 实际执行

- 已完成：全部计划改动。
- 未完成：无。

### 4. 验证结果

- pytest：122 passed（含 2 个新用例）

### 5. 结果判定

- [x] ✅ 成功

### 6. 失败根因与调整决策

- 根因：无。
- 调整决策：原料成本独立为 `material_cost` KPI，不改动既有
  `unit_cost`（人力口径），避免破坏历史报告口径。

### 7. 文档同步

- [x] 本日志已记录

### 8. 下一 Loop 输入

- 遗留问题：无。
- 下个 Loop 目标：L14 AI 自动优化（遗传算法，P2）。

## Loop L14 - AI 自动优化（遗传算法）

### 基本信息

- 日期：2026-08-11
- 负责角色：主控
- 关联功能：智能决策

### 1. 目标与验收标准

- 目标：遗传算法对人数/缓冲区/机台节拍寻优，输出 TOP 方案。
- 验收标准：
  - [x] 新增 `src/optimizer.py`（随机初始化/交叉/变异/锦标赛选择）
  - [x] 目标支持单位成本最小与总产出最大
  - [x] 输出 TOP N 含参数摘要与 KPI，同种子结果可复现
  - [x] 新增 `OptimizeDialog` 与菜单「分析 → 智能优化...」
  - [x] 全量 pytest 全绿（126 passed）

### 2. 计划改动

| 文件 | 改动内容 |
|------|----------|
| src/optimizer.py | 新增遗传算法 |
| src/gui_panels.py | OptimizeDialog |
| src/gui_main.py | 菜单接入 |
| tests/test_optimizer.py | 新增 3 用例 |

### 3. 实际执行

- 已完成：全部计划改动。
- 未完成：无。

### 4. 验证结果

- pytest：126 passed（含 4 个新用例）

### 5. 结果判定

- [x] ✅ 成功

### 6. 失败根因与调整决策

- 初版按目标方向排序导致单位成本目标升序错误；
  统一改为按适应度分数降序（分数已含目标方向）。

### 7. 文档同步

- [x] 本日志已记录

### 8. 下一 Loop 输入

- 遗留问题：无。
- 下个 Loop 目标：L15 PyInstaller 平台打包（macOS App / Windows EXE）。

## Loop L15 - PyInstaller 平台打包

### 基本信息

- 日期：2026-08-11
- 负责角色：主控
- 关联功能：交付打包

### 1. 目标与验收标准

- 目标：提供 PyInstaller 打包配置与 CI 构建通道。
- 验收标准：
  - [x] 新增 `packaging/e_pod_line_simulator.spec`（资源/隐藏导入/BUNDLE）
  - [x] requirements-dev 增加 pyinstaller
  - [x] packaging.yml 新增 macOS App 构建任务（tag/手动触发，
    上传 Artifact 并附到 Release）
  - [x] 本地构建验证通过：`E-Pod-Line-Simulator.app`（73MB）
  - [x] 本地 macOS 交付包 `E-Pod-Line-Simulator-v3.2.0-macOS.zip`（36MB）

### 2. 计划改动

| 文件 | 改动内容 |
|------|----------|
| packaging/e_pod_line_simulator.spec | 新增 |
| requirements-dev.txt | pyinstaller |
| .github/workflows/packaging.yml | build-app 任务 |
| .gitignore | build/ dist/ |

### 3. 实际执行

- 已完成：全部计划改动。
- 未完成：Windows EXE（CI 可在后续扩展 windows-latest 任务）。

### 4. 验证结果

- 本地构建成功（codesign 警告为环境性，应用可运行）
- pytest：126 passed（无代码变更）

### 5. 结果判定

- [x] ✅ 成功

### 6. 失败根因与调整决策

- spec 内相对路径指向 packaging/ 导致 run.py 找不到；
  改为基于 `SPECPATH` 定位项目根目录。
- 基础 conda 环境存在废弃 pathlib 包与 PyInstaller 冲突；
  本地验证使用临时虚拟环境，未污染用户环境。

### 7. 文档同步

- [x] 本日志已记录

### 8. 下一 Loop 输入

- 遗留问题：Windows EXE 构建（后续扩展）。
- 下个 Loop 目标：L16 全量回归与 v3.2.0 定版发布。
